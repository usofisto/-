# Восстановление + Обучение C++

Этот репозиторий создан как личное пространство для восстановления от выгорания при изучении программирования и углубления практических навыков C++.

## Основные идеи и принципы

- **С самого нуля**: Все проекты пишутся полностью самостоятельно. Никаких готовых шаблонов, копирования кода или сторонних фреймворков на начальных этапах. Только собственный код, собственные архитектурные решения и глубокое понимание каждой строчки.
- **Борьба с выгоранием**: Спокойное написание проектов в комфортном темпе, регулярная фиксация прогресса через осмысленные коммиты, создание работающих и интересных программ для души.
- **Текущий уровень и цели**: Текущий уровень оценивается как Junior. Основная цель — планомерное развитие до уровня Middle.
- **Долгосрочное обязательство**: Этот репозиторий развивается на постоянной основе и никогда не будет заброшен.

---

## Текущий проект: Графический RPG Квест (Legends of Green Meadow)

Ролевая игра с графическим интерфейсом на базе библиотеки **Raylib**. Игрок создает уникального персонажа, выбирает класс, исследует живописные локации, собирает ресурсы и участвует в случайных встречах и пошаговых битвах.

### Пошаговый план разработки

1. **Шаг 1: Ядро и Игрок** [ВЫПОЛНЕНО]
   - Базовая структура `Player` (имя, здоровье, урон, золото).
   - Простой текстовый цикл ввода-вывода.
2. **Шаг 2: Система локаций и биомы** [ВЫПОЛНЕНО]
   - Перемещение между безопасным лагерем и биомом «Зеленая Поляна».
   - Интерактивное окружение (осмотр местности, сбор лечебных трав, поиск золотых цветов).
   - Случайная встреча со Слизью (уникальная механика приручения с 80% шансом успеха и 20% риском гибели, либо стандартный бой).
3. **Шаг 3: Переход на графический интерфейс (GUI)** [ВЫПОЛНЕНО]
   - Полная интеграция с графической библиотекой **Raylib 5.0**.
   - Реализация конечного автомата состояний (`GameState`) для неблокирующих переходов между сценами.
   - Разработка темного игрового интерфейса:
     - Экран создания персонажа с поддержкой ввода кириллицы.
     - Экран выбора класса с интерактивными визуальными карточками.
     - Информационная панель героя с анимированной полосой здоровья (HP).
     - Разделенный экран: панель действий и прокручиваемый журнал событий с цветовым выделением и эффектом плавного затухания текста.
     - Экран поражения с возможностью мгновенного перезапуска.
4. **Шаг 4: Предметы и Инвентарь** [В ПЛАНАХ]
   - Реализация структуры `Item` (название, описание, эффекты применения).
   - Менеджер инвентаря на базе `std::vector<Item>` и его интеграция в GUI.
5. **Шаг 5: Враги и пошаговая боевая система** [В ПЛАНАХ]
   - Добавление различных видов врагов (`Enemy`) со своими характеристиками.
   - Полноценная боевая сцена в GUI (атака, побег, использование предметов).
6. **Шаг 6: Сохранение и загрузка прогресса** [В ПЛАНАХ]
   - Сериализация состояния игрока и мира в файл `save.txt` и автозагрузка при старте.

---

## Инструкция по запуску

### Требования к огружению
- **ОС**: Windows 10/11
- **Компилятор**: `g++` (MinGW-w64), путь к которому добавлен в системную переменную `PATH`.
- **Интерпретатор**: PowerShell (для запуска скриптов автоматизации).

### Сборка и запуск
1. **Загрузка зависимостей**:
   Скрипт автоматически скачает и распакует нужную версию Raylib 5.0 в директорию `raylib_dep`:
   ```powershell
   .\download_raylib.ps1
   ```
2. **Компиляция**:
   Соберите исполняемый файл с помощью скрипта сборки:
   ```powershell
   .\compile.ps1
   ```
3. **Запуск игры**:
   Запустите скомпилированную игру:
   ```powershell
   .\main.exe
   ```

---

## Лог разработки (Дневник проекта)

- **23.06.2026**:
  - Официальный старт проекта. Выбран жанр «Текстовая RPG с постепенным переходом в GUI».
  - Составлен верхнеуровневый план разработки и определены основные архитектурные ориентиры.

- **24.06.2026**:
  - Создана первая модульная структура исходного кода: разделение проекта на файлы `player.h`, `biom.h`, `biom.cc` и `main.cpp`.
  - Реализован базовый класс `Player` с отслеживанием характеристик и жизненного статуса.
  - Разработан первый биом «Зеленая Поляна» с текстовым интерактивным окружением (осмотр, сбор лечебных трав, поиск золота).
  - Написана логика случайной встречи со Слизью (приручение с риском мгновенной смерти персонажа или стандартное сражение за золото).
  - Решена проблема с некорректным отображением кириллицы в консоли Windows (принудительный перевод в UTF-8, Code Page 65001).

- **25.06.2026** (Сегодня):
  - **Полный переход на графический интерфейс**: Интегрирована библиотека **Raylib 5.0**. Переписан основной цикл игры под отрисовку графического окна.
  - **Интерфейс и Анимации**: Создан стильный темный интерфейс. Реализованы экраны генерации персонажа (с поддержкой ввода на русском языке), выбора класса (визуальные карточки), анимированная шкала здоровья (HP-bar), прокручиваемый лог событий с плавным затуханием старых сообщений и экран поражения.
  - **Асинхронная логика**: Весь код реструктурирован под конечный автомат состояний (`GameState`) для неблокирующих асинхронных игровых переходов и плавного отклика интерфейса.
  - **Скрипты автоматизации**: Написаны PowerShell-скрипты `download_raylib.ps1` (для скачивания и подготовки библиотек) и `compile.ps1` (для быстрой статической компиляции игры с Raylib).
  - **Реорганизация архитектуры**: Файлы исходного кода (`main.cpp`, `biom.cc`, `biom.h`, `player.h`) перенесены из корня проекта в выделенную папку `src/`. Обновлен скрипт сборки `compile.ps1` для корректной работы с новой структурой.
  - **Документирование и исправление README**: Полностью исправлен файл `README.md` (устранены дублирующиеся секции, решены проблемы с поврежденной кодировкой DOS/CP866, структура приведена к профессиональному виду).
  
  > [!IMPORTANT]
  > **Важное решение по разработке**: В связи с быстрым ростом объема исходного кода и усложнением логики интерфейса Raylib, принято стратегическое решение: **систематически использовать развернутые комментарии внутри кода для всех ключевых функций, переходов состояний и элементов интерфейса.** Это необходимо, чтобы избежать логических ошибок при масштабировании игры, сделать процесс разработки максимально наглядным и упростить дальнейшее расширение проекта (добавление инвентаря и боевой системы).
